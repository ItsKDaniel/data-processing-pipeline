redis:
  topic.name: _messages_

socket:
  topic: /topic/publish

spring:
  application:
    name: "falcon-data-pipeline"
  redis:
    database: 0
    host: localhost
    port: 6379
logging:
  level:
    io.falcon.assignment.service: debug
server:
  port: 8080
